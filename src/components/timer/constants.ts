import { TimerValue } from 'components/Timer/types';

export const MAX_TIME_UNIT_LENGTH = 2;
export const MAX_SECONDS_AND_MINUTES = 60;

export const SECONDS_IN_HOURS = 3600;
export const SECONDS_IN_MINUTES = 60;

export const INITIAL_TIMER_VALUE: TimerValue = {
  hours: '00',
  minutes: '00',
  seconds: '00',
};
